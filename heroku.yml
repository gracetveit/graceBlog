setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile
  config:
    EMAIL: grace.tveit@gmail.com
release:
  command:
    - npm run heroku:release
  image: web
run:
  web: npm run heroku:run
